const ListsBar = () =>{
    const listBar = document.createElement('div');
    listBar.classList.add('list-bar');
    listBar.setAttribute('id', 'list-bar');

    return listBar;
}

export default ListsBar;